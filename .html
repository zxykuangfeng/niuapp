<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="renderer" content="webkit">
    <title>小金县公安局</title>
    <link rel="stylesheet" href="__TMPL__Public/new/assets/css/base.css" />
    <link rel="stylesheet" type="text/css" href="__PUBLIC__/style/css.css" />
    <link rel="stylesheet" type="text/css" href="__TMPL__Public/new/assets/css/mthis.css" />
    <script type="text/javascript" src="__PUBLIC__/js/jquery.js"></script>
    <script src="__TMPL__Public/new/assets/js/jquery.SuperSlide.2.1.1.js" type="text/javascript" charset="utf-8"></script>
    <script src="__TMPL__Public/new/assets/js/main.js" type="text/javascript" charset="utf-8"></script>
    <script src="__TMPL__Public/new/assets/js/jq_scroll.js" type="text/javascript"></script>

    <php>

        if(isset($index_bg_on) && $index_bg_on && !empty($index_bg_img)){
        $bg_img = sp_get_image_preview_url($index_bg_img);
    </php>
    <style type="text/css">

        .container{
            padding-top: {$site_down_size|default=0}px;

        }
        .container .bg-top,.content .bg-top {
            background: url('<?php echo $bg_img;?>') center 0 no-repeat !important;
            height: 350px !important;
            z-index: 0 !important;
        }


        .header_top_img{
            background-image: url(/public/images/bg_top_part2.jpg);
            height:250px;
        }
        body {
            background: #daebfd;
        }
        .link-tabs {
    margin-top: 20px;
    text-align: center;
    position: relative; /* 关键：为弹窗定位做基础 */
}

.link-btn {
    margin: 0 10px;
    padding: 8px 16px;
    background: #005bac;
    color: #fff;
    border: none;
    cursor: pointer;
    border-radius: 4px;
}

/* 改为相对于 .link-tabs 的浮动弹窗 */
.popup-box {
    position: absolute;
    top: 45px; /* 控制离按钮多远 */
    left: 50%;
    transform: translateX(-50%);
    width: 80%;
    background: #fff;
    box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    z-index: 999;
    border-radius: 6px;
    padding: 20px;
    border: 1px solid #ccc;
}

.popup-content {
    position: relative;
}

.popup-content .close-btn {
    position: absolute;
    right: 15px;
    top: 5px;
    font-size: 18px;
    cursor: pointer;
}

/* 链接小方块 */
.link-box-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 30px;
    justify-content: center;
}

.link-box-grid div {
    border: 1px solid #0071c5;
    background: #fff;
    padding: 8px 12px;
    border-radius: 4px;
    white-space: nowrap;
}

.link-box-grid div a {
    color: #0071c5;
    text-decoration: none;
}



    </style>
    <php>}</php>

    <style type="text/css">
        /* advbox  居中 right 40% */
        *html,*html body{background-image:url(about:blank);background-attachment:fixed;}
        *html .advbox{position:absolute;top:expression(eval(document.documentElement.scrollTop));}
        .advbox{position:fixed;display:none;right:0%;top:0;/*margin:-215px 0 0 -325px;*/z-index: 20;}
        .advbox .advpic{position:relative;overflow:hidden;}
        .advbox .advpic .closebtn{display:block;width:60px;height:22px;line-height:26px;font-size:12px;color:#fff;text-indent:12px;overflow:hidden;position:absolute;right:12px;top:5px;z-index:99;}
    </style>
</head>
<body>


<div class="container">
    <div class="bg-top"></div>
    <div class="main">
        <!--header-->
        <div class="header clearfix">
            <php>

                if(!isset($index_bg_on) && !$index_bg_on && empty($index_bg_img)){
            </php>
            	<div class="h_top">
                    <div class="h_collect"><a href οnclick="this.style.behavior='url(#default#homepage)';this.setHomePage(window.location.href);">设为首页</a> | <a href="javascript:window.external.AddFavorite(window.location.href, '盐都公安')">加入收藏</a></div>
                    <div class="h_date">{:sp_today_fmt()}</div>
                </div>
            <php>}</php>


            <div class="logo">
                <a href="/"><img src="__TMPL__Public/new/assets/images/logo.png" alt=""></a>
            </div>
            <div class="scr-top">
                <div class="bd">
                    <ul>
                       <!--<li><img src="__TMPL__Public/assets/images/top_banner_2.png"/></li>-->
                        <!--<li><img src="__TMPL__Public/assets/images/top_banner_1.png"/></li>-->
                        <!--<li><img src="__TMPL__Public/assets/images/top_banner_4.png"/></li>-->
                    </ul>
                </div>
            </div>
        </div>
        <!--header E-->
        <!--nav S-->

        <div class="navbar">
            <ul class="clearfix">
                <php>
                    $navs = sp_get_main_nav();
                    foreach( $navs as $key => $val ){
                </php>
                <li class="nli" <php>if($key==0){</php>class="on"<php>}</php>><a href="{$val.href}" target="{$val.target}">{$val.label}</a></li>
                <php>}</php>
            </ul>
            <div class="srhbox">
                <form action="{:U('Portal/Search/index')}" method="get" target="_blank">
                    <input type="text" class="txt" name="keyword" placeholder="请输入关键字搜索" />
                    <input type="submit" class="btn" value="" />
                </form>
            </div>
        </div>
        <script type="text/javascript">
			jQuery(".navbar").slide({ 
				type:"menu",// 效果类型，针对菜单/导航而引入的参数（默认slide）
				titCell:".nli", //鼠标触发对象
				delayTime:30, //效果时间
				triggerTime:0, //鼠标延迟触发时间（默认150）
				returnDefault:true //鼠标移走后返回默认状态，例如默认频道是“预告片”，鼠标移走后会返回“预告片”（默认false）
			});
		</script>
           <!--网站导航-->
    	<php>
            $navs = sp_get_main_nav();
            //全国
            $link_1 = M('links')->order(array("listorder"=>"ASC"))->where(array('link_class'=>0,'link_status'=>1))->select();
            //全省
            $link_2 = M('links')->order(array("listorder"=>"ASC","link_id"=>"ASC"))->where(array('link_class'=>1,'link_status'=>1))->select();
            //
            $link_3 = M('links')->order(array("listorder"=>"ASC","link_id"=>"ASC"))->where(array('link_class'=>2,'link_status'=>1))->select();
            //
            $link_4 = M('links')->order(array("listorder"=>"ASC"))->where(array('link_class'=>3,'link_status'=>1))->select();

        </php>

<div class="link-tabs">
    <button class="link-btn" data-type="1">全国</button>
    <button class="link-btn" data-type="2">全省</button>
    <button class="link-btn" data-type="3">全州</button>
    <button class="link-btn" data-type="4">州局机关</button>
</div>

<!-- 悬浮弹窗容器 -->
<div id="linkPopup" class="popup-box" style="display: none;">
    <div class="popup-content">
        <span class="close-btn">×</span>
        <ul id="linkList"></ul>
    </div>
</div>
		<script type="text/javascript">
		    const linkMap = {
    1: <?php echo json_encode($link_1); ?>,
    2: <?php echo json_encode($link_2); ?>,
    3: <?php echo json_encode($link_3); ?>,
    4: <?php echo json_encode($link_4); ?>
};

$('.link-btn').on('click', function () {
    const type = $(this).data('type');
    const links = linkMap[type] || [];
    let html = '';
    links.forEach(item => {
        html += `<div><a href="${item.link_url}" target="_blank">${item.link_name}</a></div>`;
    });
    $('#linkList').html(html);
    $('#linkPopup').stop(true, true).slideDown();
});

// 关闭弹窗
$('.close-btn').on('click', function () {
    $('#linkPopup').slideUp();
});

// 点击其他区域关闭弹窗（可选）
$(document).on('click', function (e) {
    if (!$(e.target).closest('#linkPopup, .link-btn').length) {
        $('#linkPopup').slideUp();
    }
});
		</script>
        <!--nav E-->
